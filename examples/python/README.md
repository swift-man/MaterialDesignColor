# Python Example

Placeholder for Python examples that depend on `packages/python`.

Use `create_theme(preset="expressive")` and `theme.color_scheme` for Material 3 preset role colors. Use `create_theme(color_scheme={"primary": "#6750A4"})` to inject Material Theme Builder role colors.
