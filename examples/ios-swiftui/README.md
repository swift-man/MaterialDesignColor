# iOS SwiftUI Example

This folder contains the SwiftUI preview source that used to live inside the library target.

Use it from an app or preview target that depends on `MaterialDesignColorSwiftUI`.
